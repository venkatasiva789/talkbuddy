services:
  - type: web
    name: talkbuddy
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.6"
